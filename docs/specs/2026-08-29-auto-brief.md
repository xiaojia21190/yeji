# 自动业绩报告（GitHub Actions 定时扫描）

- 日期：2026-08-29（v3：完整结构版）
- 状态：已实现（山东黄金/深振业Ａ/全新好真实端到端验收，judge 视觉验收通过）

## 1. 目标

定时监听**全 A 股**财报披露，新披露自动生成**完整结构报告**（评分卡 + 前景与催化 +
预期反推 + 趋势 + 估值 + 同行），发布到报告中心（Pages 自动更新）并开 issue 通知。
数字全部脚本可复算；定性段落由 AI 撰写并显著标注「未经搜索溯源」——这是与本地
完整 skill 报告（含 WebSearch 溯源）的诚实边界。

## 2. 架构与分工（v3）

```
cron (UTC 2/10/14 点 = 北京 10/18/22 点)
  └─ scan_and_brief.py
       ├─ scan_dev.scan_full_market()   # 巨潮预约披露表 1 次调用（当前季 ±1 期）
       │    → 回看窗口（3 天）内「实际披露」名单 → watchlist 优先、confirm_cap=300 截断
       ├─ auto_brief_dev.build_brief(period=日历期)   # 新浪未同步该期 → 跳过计数，下轮重试
       │    ├─ 脚本量化：单季拆解/同比、评分卡数值规则、反推对照（仅披露日前研报）、
       │    │   趋势、估值
       │    └─ AI（单次调用生成全部定性段）：summary 文字、驱动维定性、前景与催化
       │        （核心优势/订单前瞻/走势因素）、同行提名（脚本取数验证）、估值区间判断
       ├─ save_payload → reports/{code}_{period}.json（viewer/Pages 直接渲染）
       └─ scan_result.json → workflow 建 issue 通知（含积压与未同步计数）

降级：日历接口整体失败 → 回退 watchlist 逐只扫描（摘要最新期判定）；
AI 缺席/失败 → 纯量化结构（无 outlook/同行模块），驱动维缺省按 warn 计 0。
单次上限 40 份（watchlist 不受限），积压顺延下一轮。
```

纪律：AI 只允许使用脚本算好的数据包数字（schema 校验，编造即整体降级）；
summary tone 与综合评级按 performance.md 合成规则可复算，AI 只写文字不定 tone；
AI 定性模块的表格标题与免责声明固定携带「AI 推断·未经搜索溯源」标注。

## 3. 配置

- `watchlist.json`：code/name/peers（配置了 peers 的公司同行表用配置名单，否则 AI 提名）
- 仓库 Secrets（Settings → Secrets and variables → Actions）：
  - `AI_API_KEY`（必需才启用 AI；不配则纯量化）
  - `AI_BASE_URL`（如 https://ai.huan666.de/v1）
  - `AI_MODEL`（如 deepseek-v4-flash）
- 手动触发：Actions 页 scan-earnings → Run workflow（调试入口）

## 4. 验收中发现并修复的坑

1. 摘要接口毛利率/ROE 是**百分数值**（24.43 = 24.43%），与自算小数混比产生 1801% 脏值——统一 ÷100
2. prev_cum_period 漏拼年份 → 单季退化成累计
3. scan() 未透传 peers → 同行模块静默消失
4. AI tone 覆盖合成规则 → 收紧为 AI 只写文字
5. 跨行字符串少 `+` 被解析为 str 调用（TypeError）
6. 巨潮期数参数「一季/三季」**不带「报」**（半年报/年报才带）
7. **负增长阈值守卫**：营收同比为负时，应收/存货「增速 ×1.5/×2」阈值为负形同虚设（存货 −4.5% 被判高增）——仅营收正增长时启用
8. **亏损期口径**：归母为负时净现比/非经常占比失真（分母为负），不参与质量打分并注明
9. cutoff 字符串二次 isoformat（TypeError）；日历整体失败自动降级 watchlist 逐只扫描
10. AI 段改名 ai_full 后残留旧引用（NameError）——网关把报错行混进 tqdm 物理行导致日志被 grep 误滤，落盘日志才定位到
11. AI 空返回（网关偶发 content=""）→ 视为失败参与重试
12. 中文长 JSON 超 max_tokens 被截断成非法 JSON → 上限提至 4000 并收紧输出字数要求
13. sections 动态编号在列表字面量内重复求值 → 趋势/估值同号——改为显式 n2/n2+1/n2+2

## 5. 全市场实跑记录（2026-08-29，中报季峰值）

日历命中 2958 家（回看 3 天）→ 待生成 300（confirm_cap 截断）→ 抽样 3 份全部升级为
七模块完整结构：600547 山东黄金（watchlist：AI 前景+同行+估值注，judge 验收通过）、
000006 深振业Ａ（亏损地产边界样本）、000007 全新好。边界降级均正确：
无可溯源预测 → 预期对照跳过；亏损 → 质量口径不适用；AI 失败 → 纯量化结构。

## 6. 边界

- 反推季节系数为公司上年历史节奏（无机构校准）；扫描仅覆盖 A 股正式披露（预告不触发）
- AI 段落无 WebSearch 溯源：外资行目标价、解禁减持、在手订单等需本地完整 skill 补齐
- 免责声明固定标注「未经搜索溯源与人工复核」
